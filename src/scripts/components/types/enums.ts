export enum InputType {
  text = 'text',
  submit = 'submit',
  button = 'button',
  password = 'password',
  radio = 'radio',
  checkbox = 'checkbox',
  reset = 'reset',
}

export enum HeadingType {
  h1 = 'h1',
  h2 = 'h2',
  h3 = 'h3',
  h4 = 'h4',
  h5 = 'h5',
  h6 = 'h6',
}

export enum BlockType {
  header = 'header',
  footer = 'footer',
  main = 'main',
  aside = 'aside',
  nav = 'nav',
  article = 'article',
  section = 'section',
  div = 'div',
}

export enum Pages {
  index = 'index',
  login = 'login',
  registration = 'registration',
  user = 'user',
  notFound = 'not-found',
}
